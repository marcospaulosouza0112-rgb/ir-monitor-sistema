services:
  - type: web
    name: ir-monitor
    runtime: python
    buildCommand: "pip install flask flask-cors gunicorn requests"
    startCommand: "gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
